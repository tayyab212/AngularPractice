import { StudentspipePipe } from './studentspipe.pipe';

describe('StudentspipePipe', () => {
  it('create an instance', () => {
    const pipe = new StudentspipePipe();
    expect(pipe).toBeTruthy();
  });
});
