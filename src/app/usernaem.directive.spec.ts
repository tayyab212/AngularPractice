import { UsernaemDirective } from './usernaem.directive';

describe('UsernaemDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernaemDirective();
    expect(directive).toBeTruthy();
  });
});
